---
description: 能力包/模块边界：闭环归属，禁止把「沾一点 A」的 B 侧代码塞进 A
alwaysApply: true
---

# 模块闭环边界（路 1 拆包）

## 原则

- **A 模块只放 A 自己的闭环**：独立窗口、本域 main（表/同步/IPC）、本域类型与本域 API。
- **禁止**：B（如宿主主窗）里「沾一点 A」的逻辑，也硬塞进 A 包——那是过度设计。
- **允许重复**：B 为完成自己业务可自写一份轻量调用/本地 store；不必为复用去 import A。

## 正例 / 反例（圈子）

```text
✅ app-circle：圈子独立窗口 renderer + 圈子 main activate（建表/datasync/WS/IPC）
✅ 宿主主窗：会话列表圈子资料 pinia、主窗通知路由、聊天名片 HTTP —— 留在宿主
❌ 把主窗 useCircleStore / 名片 API 塞进 app-circle，再让宿主反向 import
❌ 为「宿主不点名业务」在渲染侧大搞 registry / 贡献点（本项目不需要）
```

## 拆包时怎么划

| 放进能力包 | 留在宿主（或其它业务模块） |
|------------|----------------------------|
| 该能力的独立 BrowserWindow 与其 renderer | 主窗组合层里用到该能力的会话展示、入口、名片等 |
| 该能力主进程自有管道注册 | 核心 IM（chat/friend/group）与主窗 pinia |
| 该能力领域类型 / 本窗 API | 仅为主窗服务的薄封装（可与包内 API 重复） |

## 主进程 vs 渲染

- **主进程**：宿主 MessageManager/DataSync/建表等管道用 activate + registry 反转依赖 —— 合理。
- **渲染进程**：宿主直接组合；需要数据就本地有/没有为空；不要为解耦再加一层注册表。
