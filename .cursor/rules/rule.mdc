---
alwaysApply: true
---
# 项目概述

这是一个开源的IM（即时通讯）项目，对标大厂和开源IM产品。

### 技术架构
vite+ eluniappectron+vue3

### ts别名

### 文件命名
- 所有的文件不能以大写字母开头， 如果是多个单词采用驼峰命名
- 文件名字如果我文件夹已经携带了xx， 这个子文件就不要在携带xxx开头的了

### css的编写
- 我使用less， 你的css开发的时候采用嵌套结构

### pinia规范
- 如下参考模版, 请替换usexxStore中的xx为对应的模块名， 比如useuserStore
```typescript
import { defineStore } from 'pinia'

export const usexxStore = defineStore('usexxStore', {
  state: () => ({
  }),

  getters: {
  },

  actions: {
  },
})
```

### 接口规范
- 所有接口返回的数据结构都是
```typescript
{
  code: number,
  msg: string,
  result: any
}
```
- 接口的type是以I开头的， res和req结尾鞥多
- 接口的方法名字需要加Api
- 接口没用报错，不需要catch，code为0是正常，非0是异常的, 如果异常则使用msg属性
- import * as 文件名Api from '@/api/文件名' 不允许这么写
- 我的接口没有异常的情况，不要写catch

### 切记
- 不要过度开发
- 不要写md文件
- 尽可能使用别名不要使用../之类的相对路径

### .vue文件编写规范
- 不要 script lang="ts" setup 这种组件， 采用export default defineComponent这样的代码